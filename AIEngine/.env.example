# Copy to .env and fill in your key. .env is git-ignored.
OPENAI_API_KEY=sk-open-ai-api-key

# Cheapest text model in the gpt-*-mini class is a good default.
OPENAI_MODEL=gpt-4o-mini

# Reject uploads larger than this (megabytes).
MAX_UPLOAD_MB=10
